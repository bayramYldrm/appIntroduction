<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome!</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        body {
            margin: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: #333;
            line-height: 1.6;
        }
        .navbar {
            background: #004d40;
            color: #fff;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 30px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }
        .navbar .logo {
            font-size: 28px;
            font-weight: bold;
        }
        .navbar .nav-links {
            display: flex;
            gap: 20px;
        }
        .navbar .nav-links a {
            color: #fff;
            text-decoration: none;
            font-size: 16px;
            transition: color 0.3s ease;
        }
        .navbar .nav-links a:hover {
            color: #f1c40f;
        }
        .carousel {
            position: relative;
            width: 100%;
            height: 600px;
            overflow: hidden;
        }

        .carousel-inner {
            height: 100%;
        }

        .carousel-item {
            height: 100%;
        }

        .carousel-item img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            object-position: center;
            display: block;
        }

        .carousel-control-prev, .carousel-control-next {
            width: 5%;
            height: 100%;
            top: 0;
            bottom: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #fff;
            background-color: rgba(0, 0, 0, 0.5);
            z-index: 10;
        }

        .carousel-control-prev {
            left: 0;
        }

        .carousel-control-next {
            right: 0;
        }

        .content-section {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            justify-content: center;
            padding: 30px;
            background-color: #f8f9fa;
        }

        .content-section .content-item {
            background: #fff;
            border-radius: 12px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 20px;
            text-align: center;
            width: 300px;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .content-section .content-item:hover {
            transform: translateY(-10px);
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
        }

        .content-item h3 {
            margin-bottom: 10px;
            font-size: 24px;
            color: #333;
        }

        .content-item p {
            margin-bottom: 0;
            font-size: 16px;
            color: #666;
        }

        .product-slider {
            position: relative;
            width: 100%;
            padding: 20px 0;
            background-color: #f8f9fa;
        }

        .product-cards {
            display: flex;
            overflow-x: auto;
            scroll-behavior: smooth;
            padding: 10px;
            gap: 20px;
        }

        .product-card {
            background: #fff;
            border-radius: 12px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            width: 300px;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            flex: 0 0 auto;
        }

        .product-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
        }

        .product-card img {
            width: 200px;
            height: 200px;
            object-fit: cover;
        }

        .product-card:hover img {
            opacity: 0.8;
        }

        .product-card .content {
            padding: 20px;
        }

        .product-card .content h3 {
            margin: 0 0 10px;
            font-size: 22px;
            color: #333;
        }

        .product-card .content p {
            margin: 0;
            font-size: 16px;
            color: #666;
        }

        .footer {
            background: #00695c;
            color: #fff;
            text-align: center;
            padding: 20px;
            box-shadow: 0 -4px 8px rgba(0, 0, 0, 0.2);
        }

        .scroll-button {
            position: absolute;
            top: 50%;
            transform: translateY(-50%);
            background-color: rgba(0, 0, 0, 0.5);
            color: #fff;
            border: none;
            padding: 10px;
            cursor: pointer;
            z-index: 10;
        }

        .scroll-left {
            left: 10px;
        }

        .scroll-right {
            right: 10px;
        }

        .footer {
            background-color: #00695c; /* Dark background color */
        }
        .social-media-icons .btn {
            border-radius: .25rem;
            font-size: 1.25rem;
        }
        .social-media-icons .btn i {
            transition: color 0.3s ease;
        }
        .social-media-icons .btn:hover i {
            color: #007bff; /* Change color on hover */
        }
    </style>
</head>
<body>
<div class="navbar">
    <div class="logo">
        <img src="/uploads/images/LOGO1.png" alt="Logo" style="width: 150px; height: auto;">
    </div>
    <div class="nav-links">
        <a href="/about">Hakkımızda</a>
        <a href="/contact">İletişim</a>
    </div>
</div>

<div id="carouselExampleIndicators" class="carousel slide">
    <ol class="carousel-indicators">
        {% for home in homes %}
            <li data-target="#carouselExampleIndicators" data-slide-to="{{ loop.index0 }}" class="{{ loop.first ? 'active' : '' }}"></li>
        {% endfor %}
    </ol>
    <div class="carousel-inner">
        {% for home in homes %}
            <div class="carousel-item {{ loop.first ? 'active' : '' }}">
                <img src="{{ asset('uploads/images/' ~ home.image) }}" class="d-block w-100" alt="Slide Image">
                <div class="carousel-caption d-none d-md-block">
                    <h5>{{ home.title }}</h5>
                </div>
            </div>
        {% endfor %}
    </div>
    <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
        <span class="sr-only">Previous</span>
    </a>
    <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
        <span class="carousel-control-next-icon" aria-hidden="true"></span>
        <span class="sr-only">Next</span>
    </a>
</div>

<div class="content-section">
    {% for home in homes %}
        <div class="content-item">
            <h3>{{ home.title }}</h3>
            <p class="description">{{ home.description }}</p>
        </div>
    {% endfor %}
</div>

<div class="product-slider">
    <button class="scroll-button scroll-left" onclick="scrollLeft()">&#8249;</button>
    <div class="product-cards">
        {% for app in apps %}
            <div class="product-card">
                <img src="{{ asset('uploads/images/' ~ app.image) }}" alt="{{ app.name }}" class="product-image">
                <div class="content">
                    <h3>{{ app.name }}</h3>
                    <p class="description">{{ app.description|slice(0, 80) }}{% if app.description|length > 80 %}...{% endif %}</p>
                    <a href="{{ path('app_app_show', {'id': app.id}) }}" class="btn btn-primary">Detaylar</a>
                </div>
            </div>
        {% endfor %}
    </div>
    <button class="scroll-button scroll-right" onclick="scrollRight()">&#8250;</button>
</div>


<!-- Footer Section -->
{% for about_u in aboutuses %}
    <footer class="footer  text-white py-3">
        <div class="container text-center">
            <p>&copy; 2024 {{ about_u.mainTitle }}. Tüm hakları saklıdır.</p>
            <div class="social-media-icons">
                <a href="{{ about_u.facebookLink }}" target="_blank" class="btn btn-outline-light btn-sm me-2">
                    <i class="bi bi-facebook"></i>
                </a>
                <a href="{{ about_u.twitterLink }}" target="_blank" class="btn btn-outline-light btn-sm me-2">
                    <i class="bi bi-twitter"></i>
                </a>
                <a href="{{ about_u.linkedin }}" target="_blank" class="btn btn-outline-light btn-sm">
                    <i class="bi bi-linkedin"></i>
                </a>
            </div>
        </div>
    </footer>
{% endfor %}

<script>
    function scrollLeft() {
        const container = document.querySelector('.product-cards');
        const scrollAmount = container.scrollWidth / 3; // Daha büyük bir değer deneyin
        container.scrollBy({ left: -scrollAmount, behavior: 'smooth' });
    }

    function scrollRight() {
        const container = document.querySelector('.product-cards');
        const scrollAmount = container.scrollWidth / 3; // Daha büyük bir değer deneyin
        container.scrollBy({ left: scrollAmount, behavior: 'smooth' });
    }
</script>

<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.2/dist/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
