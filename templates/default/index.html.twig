<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome!</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        body {
            margin: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: #333;
            line-height: 1.6;
        }
        .navbar {
            background: #004d40;
            color: #fff;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 30px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }
        .navbar .logo {
            font-size: 28px;
            font-weight: bold;
        }
        .navbar .nav-links {
            display: flex;
            gap: 20px;
        }
        .navbar .nav-links a {
            color: #fff;
            text-decoration: none;
            font-size: 16px;
            transition: color 0.3s ease;
        }
        .navbar .nav-links a:hover {
            color: #f1c40f;
        }
        .carousel {
            position: relative;
            width: 100%;
            height: 600px; /* İstediğiniz yüksekliği buraya ayarlayın */
            overflow: hidden;
        }

        .carousel-inner {
            height: 100%;
        }

        .carousel-item {
            height: 100%;
        }

        .carousel-item img {
            width: 100%;
            height: 100%;
            object-fit: cover; /* Resmin carouselin tamamını kaplamasını sağlar */
            object-position: center; /* Resmi ortalar */
            display: block;
        }

        /* Carousel kontrol butonları için stil ayarları */
        .carousel-control-prev, .carousel-control-next {
            width: 5%; /* Ok genişliği */
            height: 100%; /* Ok yüksekliği */
            top: 0;
            bottom: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #fff; /* Ok rengi */
            background-color: rgba(0, 0, 0, 0.5); /* Ok arka plan rengi */
            z-index: 10; /* Diğer içeriklerin üzerinde görünmesini sağlar */
        }

        .carousel-control-prev {
            left: 0;
        }

        .carousel-control-next {
            right: 0;
        }

        .product-cards {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            padding: 30px;
            justify-content: center;
        }
        .product-card {
            background: #fff;
            border-radius: 12px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            width: 300px;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }
        .product-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
        }
        .product-card img {
            width: 200px;
            height: 200px;
            object-fit: cover;
            transition: opacity 0.3s ease;
            display: block;
            margin: 0 auto; /* Resmi yatayda ortalar */
        }
        .product-card:hover img {
            opacity: 0.8;
        }
        .product-card .content {
            padding: 20px;
        }
        .product-card .content h3 {
            margin: 0 0 10px;
            font-size: 22px;
            color: #333;
        }
        .product-card .content p {
            margin: 0;
            font-size: 16px;
            color: #666;
        }
        .footer {
            background: #00695c;
            color: #fff;
            text-align: center;
            padding: 20px;
            box-shadow: 0 -4px 8px rgba(0, 0, 0, 0.2);
        }
    </style>
</head>
<body>
<div class="navbar">
    <div class="logo">
        <img src="/uploads/images/LOGO1.png" alt="Logo" style="width: 150px; height: auto;">
    </div>


    <div class="nav-links">
        <a href="#">About Us</a>
        <a href="#">Contact Us</a>
    </div>
</div>

<div id="carouselExampleIndicators" class="carousel slide">
    <ol class="carousel-indicators">
        {% for home in homes %}
            <li data-target="#carouselExampleIndicators" data-slide-to="{{ loop.index0 }}" class="{{ loop.first ? 'active' : '' }}"></li>
        {% endfor %}
    </ol>
    <div class="carousel-inner">
        {% for home in homes %}
            <div class="carousel-item {{ loop.first ? 'active' : '' }}">
                <img src="{{ asset('uploads/images/' ~ home.image) }}" class="d-block w-100" alt="Slide Image">
                <div class="carousel-caption d-none d-md-block">
                    <h5>{{ home.title }}</h5>
                </div>
            </div>
        {% endfor %}
    </div>
    <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
        <span class="sr-only">Previous</span>
    </a>
    <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
        <span class="carousel-control-next-icon" aria-hidden="true"></span>
        <span class="sr-only">Next</span>
    </a>
</div>

<div class="product-cards">
    {% for app in apps %}
        <div class="product-card">
            <img src="{{ asset('uploads/images/' ~ app.image) }}" alt="{{ app.name }}" class="product-image">
            <div class="content">
                <h3>{{ app.name }}</h3>
                <p class="description">{{ app.description }}</p>
                <a href="{{ path('app_app_show', {'id': app.id}) }}" class="btn btn-primary">Details</a>
            </div>
        </div>
    {% endfor %}
</div>


<div class="footer">
    <p>&copy; 2024 Your Company Name. All rights reserved.</p>
</div>

<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
