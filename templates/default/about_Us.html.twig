<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        body {
            margin: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: #333;
            line-height: 1.6;
        }
        .navbar {
            background: #004d40;
            color: #fff;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 30px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }
        .navbar .logo {
            font-size: 28px;
            font-weight: bold;
        }
        .navbar .nav-links {
            display: flex;
            gap: 20px;
        }
        .navbar .nav-links a {
            color: #fff;
            text-decoration: none;
            font-size: 16px;
            transition: color 0.3s ease;
        }
        .navbar .nav-links a:hover {
            color: #f1c40f;
        }

        .content-section {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            justify-content: center;
            padding: 30px;
            background-color: #f8f9fa;
        }
        .content-section .content-item {
            background: #fff;
            border-radius: 12px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 20px;
            text-align: center;
            width: 300px;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }
        .content-section .content-item:hover {
            transform: translateY(-10px);
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
        }
        .content-item h3 {
            margin-bottom: 10px;
            font-size: 24px;
            color: #333;
        }
        .content-item p {
            margin-bottom: 0;
            font-size: 16px;
            color: #666;
        }

        .main-title {
            text-align: center;
            margin: 2rem 0;
            font-size: 32px;
            color: #333;
        }
        .card-img-top {
            height: 200px;
            object-fit: cover;
        }
        .card-deck .card {
            margin-bottom: 1.5rem;
        }

        .content-section {
            padding: 60px 0;
            background-color: #f8f9fa;
        }
        .section-title {
            text-align: center;
            margin-bottom: 40px;
            font-size: 36px;
            color: #333;
            font-weight: bold;
        }
        .content-item {
            margin-bottom: 40px;
        }
        .content-item h4 {
            font-size: 28px;
            color: #007bff;
            margin-bottom: 20px;
            border-bottom: 2px solid #007bff;
            padding-bottom: 10px;
        }
        .content-item p {
            font-size: 18px;
            color: #555;
        }
        .content-item a {
            color: #007bff;
            text-decoration: none;
        }
        .content-item a:hover {
            text-decoration: underline;
        }

        .footer {
            background-color: #00695c; /* Dark background color */
        }
        .social-media-icons .btn {
            border-radius: .25rem;
            font-size: 1.25rem;
        }
        .social-media-icons .btn i {
            transition: color 0.3s ease;
        }
        .social-media-icons .btn:hover i {
            color: #007bff; /* Change color on hover */
        }
    </style>
</head>
<body>
<div class="navbar">
    <div class="logo">
        <img src="/uploads/images/LOGO1.png" alt="Logo" style="width: 150px; height: auto;">
    </div>
    <div class="nav-links">
        <a href="/about">Hakkımızda</a>
        <a href="#">İletişim</a>
    </div>
</div>

<div class="container my-4">
    <!-- Main Title -->
    {% for about_u in aboutuses %}
        <div class="section-title">{{ about_u.mainTitle }}</div>
        <p class="card-text">{{ about_u.mainDescription }}</p>
    {% endfor %}
    <!-- Card Deck -->
    <div class="row">
        {% for about_u in aboutuses %}
            <div class="col-md-4 mb-4">
                <div class="card">
                    <img src="{{ asset('uploads/' ~ about_u.adImage1) }}" class="card-img-top" alt="Image 1">
                </div>
            </div>
            <div class="col-md-4 mb-4">
                <div class="card">
                    <img src="{{ asset('uploads/' ~ about_u.adImage2) }}" class="card-img-top" alt="Image 2">
                </div>
            </div>
            <div class="col-md-4 mb-4">
                <div class="card">
                    <img src="{{ asset('uploads/' ~ about_u.adImage3) }}" class="card-img-top" alt="Image 3">
                </div>
            </div>
        {% endfor %}
    </div>
</div>


<!-- Content Section -->
<div class="content-section">
    <div class="container">
        {% for about_u in aboutuses %}
            <div class="row">
                {% if about_u.vision %}
                    <div class="col-md-4 content-item">
                        <h4>Vizyon</h4>
                        <p>{{ about_u.vision }}</p>
                    </div>
                {% endif %}
                {% if about_u.mission %}
                    <div class="col-md-4 content-item">
                        <h4>Misyon</h4>
                        <p>{{ about_u.mission }}</p>
                    </div>
                {% endif %}
                {% if about_u.founders %}
                    <div class="col-md-4 content-item">
                        <h4>Kurucular</h4>
                        <p>{{ about_u.founders }}</p>
                    </div>
                {% endif %}
                {% if about_u.history %}
                    <div class="col-md-4 content-item">
                        <h4>Tarihçe</h4>
                        <p>{{ about_u.history }}</p>
                    </div>
                {% endif %}
                {% if about_u.services %}
                    <div class="col-md-4 content-item">
                        <h4>Servisler</h4>
                        <p>{{ about_u.services }}</p>
                    </div>
                {% endif %}
                {% if about_u.phone or about_u.email or about_u.adres %}
                    <div class="col-md-4 content-item">
                        <h4>İletişim</h4>
                        {% if about_u.phone %}
                            <p>Telefon: {{ about_u.phone }}</p>
                        {% endif %}
                        {% if about_u.email %}
                            <p>Email: {{ about_u.email }}</p>
                        {% endif %}
                        {% if about_u.adres %}
                            <p>Adres: {{ about_u.adres }}</p>
                        {% endif %}
                    </div>
                {% endif %}
            </div>
            <hr>
        {% endfor %}
    </div>
</div>


<!-- Footer Section -->
{% for about_u in aboutuses %}
<footer class="footer  text-white py-3">
    <div class="container text-center">
        <p>&copy; 2024 {{ about_u.mainTitle }}. Tüm hakları saklıdır.</p>
            <div class="social-media-icons">
                    <a href="{{ about_u.facebookLink }}" target="_blank" class="btn btn-outline-light btn-sm me-2">
                        <i class="bi bi-facebook"></i>
                    </a>
                    <a href="{{ about_u.twitterLink }}" target="_blank" class="btn btn-outline-light btn-sm me-2">
                        <i class="bi bi-twitter"></i>
                    </a>
                    <a href="{{ about_u.linkedin }}" target="_blank" class="btn btn-outline-light btn-sm">
                        <i class="bi bi-linkedin"></i>
                    </a>
            </div>
    </div>
</footer>
{% endfor %}


<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.10.2/dist/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
