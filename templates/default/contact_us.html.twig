<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        body {
            margin: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: #333;
            line-height: 1.6;
            background-color: #f8f9fa;
        }
        .navbar {
            background: #004d40;
            color: #fff;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 30px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }
        .navbar .logo {
            font-size: 28px;
            font-weight: bold;
        }
        .navbar .nav-links {
            display: flex;
            gap: 20px;
        }
        .navbar .nav-links a {
            color: #fff;
            text-decoration: none;
            font-size: 16px;
            transition: color 0.3s ease;
        }
        .navbar .nav-links a:hover {
            color: #f1c40f;
        }

        .card-custom {
            margin: 2rem auto;
            padding: 2rem;
            background: #fff;
            border-radius: 12px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            max-width: 600px;
        }
        .card-custom h1 {
            font-size: 28px;
            margin-bottom: 1.5rem;
            color: #004d40;
            text-align: center;
        }
        .card-custom .btn-primary {
            background-color: #004d40;
            border: none;
        }
        .card-custom .btn-primary:hover {
            background-color: #00332a;
        }
        .footer {
            background-color: #00695c;
            padding: 20px 0;
            text-align: center;
            color: #fff;
        }
        .footer p {
            margin: 0;
        }
        .social-media-icons .btn {
            border-radius: .25rem;
            font-size: 1.25rem;
        }
        .social-media-icons .btn i {
            transition: color 0.3s ease;
        }
        .social-media-icons .btn:hover i {
            color: #007bff;
        }
    </style>
</head>
<body>
<div class="navbar">
    <div class="logo">
        <img src="/uploads/images/LOGO1.png" alt="Logo" style="width: 150px; height: auto;">
    </div>
    <div class="nav-links">
        <a href="/about">Hakkımızda</a>
        <a href="#">İletişim</a>
    </div>
</div>

<div class="container">
    <div class="card card-custom">
        <h1>Yeni ContactUs Oluştur</h1>

        {% block body %}
            {{ form_start(form) }}
            <div class="form-group">
                {{ form_label(form.firstname, 'Adınız') }}
                {{ form_widget(form.firstname, {'attr': {'class': 'form-control'}}) }}
            </div>
            <div class="form-group">
                {{ form_label(form.lastname, 'Soyadınız') }}
                {{ form_widget(form.lastname, {'attr': {'class': 'form-control'}}) }}
            </div>
            <div class="form-group">
                {{ form_label(form.email, 'E-posta') }}
                {{ form_widget(form.email, {'attr': {'class': 'form-control'}}) }}
            </div>
            <div class="form-group">
                {{ form_label(form.city, 'Şehir') }}
                {{ form_widget(form.city, {'attr': {'class': 'form-control'}}) }}
            </div>
            <div class="form-group">
                {{ form_label(form.message, 'Mesajınız') }}
                {{ form_widget(form.message, {'attr': {'class': 'form-control', 'rows': 4}}) }}
            </div>
            <div class="text-center mt-4">
                <button type="submit" class="btn btn-primary">Kaydet</button>
                <a href="{{ path('app_default') }}" class="btn btn-secondary">Geri Dön</a>
            </div>
            {{ form_end(form) }}
        {% endblock %}
    </div>
</div>

<!-- Footer Section -->
{% for about_u in aboutuses %}
    <footer class="footer">
        <div class="container text-center">
            <p>&copy; 2024 {{ about_u.mainTitle }}. Tüm hakları saklıdır.</p>
            <div class="social-media-icons">
                <a href="{{ about_u.facebookLink }}" target="_blank" class="btn btn-outline-light btn-sm me-2">
                    <i class="bi bi-facebook"></i>
                </a>
                <a href="{{ about_u.twitterLink }}" target="_blank" class="btn btn-outline-light btn-sm me-2">
                    <i class="bi bi-twitter"></i>
                </a>
                <a href="{{ about_u.linkedin }}" target="_blank" class="btn btn-outline-light btn-sm">
                    <i class="bi bi-linkedin"></i>
                </a>
            </div>
        </div>
    </footer>
{% endfor %}


<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.10.2/dist/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
