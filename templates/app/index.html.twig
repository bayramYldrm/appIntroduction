{% extends 'base.html.twig' %}

{% block title %}App index{% endblock %}

{% block body %}
    <h1>App index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Description</th>
                <th>IosLink</th>
                <th>WindowsLink</th>
                <th>PlayStoreLink</th>
                <th>Image</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for app in apps %}
            <tr>
                <td>{{ app.id }}</td>
                <td>{{ app.name }}</td>
                <td>{{ app.description }}</td>
                <td>{{ app.iosLink }}</td>
                <td>{{ app.windowsLink }}</td>
                <td>{{ app.playStoreLink }}</td>
                <td>{{ app.image }}</td>
                <td>
                    <a href="{{ path('app_app_show', {'id': app.id}) }}">show</a>
                    <a href="{{ path('app_app_edit', {'id': app.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="9">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_app_new') }}">Create new</a>
{% endblock %}
